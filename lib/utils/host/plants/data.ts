// This file is kept for backwards compatibility but is no longer used
// All host plant data now comes from /api/host/dashboard which queries the projects table

// Placeholder export to keep this as a valid module
export const DATA_MIGRATED = true;
